package com.projecki.fusion.reward;

import org.bukkit.entity.Player;

/**
 * Simple implementation of the generic reward type resolving the generic type to 'Player'
 */
public interface Reward extends GenericReward<Player> {
}
